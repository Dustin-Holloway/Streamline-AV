/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Streamline-AV", // 👈 Use your repo name here
  trailingSlash: true, // 👈 Ensures links resolve correctly on GitHub Pages
};

export default nextConfig;
